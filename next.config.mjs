/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:['cdn1.iconfinder.com', "encrypted-tbn0.gstatic.com"] // it is allow to user show where it images exists in website and hostname as well
    }
};

export default nextConfig;
